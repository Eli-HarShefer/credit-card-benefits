# מקורות הנתונים — איך לרענן

עודכן: 2026-08-15

הקטלוגים זזים כל הזמן (המשתמש ביקש רענון שבועי). המסמך הזה הוא ההוראות המדויקות
לשחזור הקצירה, כולל ה-endpoints וה-headers שגילינו.

---

## 1. בהצדעה — קטלוג המועדון

**כתובת:** `https://www.behatsdaa.org.il`
**גישה:** דורש התחברות (OTP לנייד + מייל). האתר SPA — WebFetch מחזיר דף ריק.
חייבים דפדפן מחובר.

### ה-API

```
GET https://back.behatsdaa.org.il/api/category/GetCategoryById?categoryId=<id>
```

**Headers חובה** — בלעדיהם מוחזרת שגיאת ‎.NET‎ (`Autofac.Co...`):

| Header | ערך |
|---|---|
| `Accept` | `application/json, text/plain, */*` |
| `OrganizationId` | `20` |
| `native` | `true` |

התשובה: `{status, data}`. השדות המעניינים ב-`data`:

| שדה | מה זה |
|---|---|
| `categoryName` | שם הפריט/הקטגוריה |
| `isLeaf` | האם זה פריט סופי או ענף |
| `subCategories[]` | ילדים — **כל ילד כולל `isLeaf` משלו**, ולכן אפשר לזחול בלי בקשה מיותרת |
| `redimType` | **מנגנון המימוש** — טקסט חופשי שמתאר איך מממשים (קופון ב-SMS, הצגה בבית העסק וכו') |
| `description` | בד"כ המחיר ("החל מ-105 ₪") |
| `campaignDetails` | הגבלות ("מוגבל לרכישה אחת ללקוח בחודש") |
| `remarks`, `mustKnow`, `termsOfUse` | תנאים |
| `locations[]`, `business` | סניפים ובית העסק |

### מבנה הניווט

- לובי קטגוריה: `/category/lobby/<id>`
- דף פריטים: `/category/products/<id>`
- חיפוש: `/category/searchCategory/<מונח>`

פריטי הניווט העליון הם `div.by-top-nav-bar-middle-item` (לא קישורים — click handlers).

### מזהי השורש

| קטגוריה | id |
|---|---|
| צרכנות | 1590 |
| מבצעי רכב | 104534 |
| מופעים והצגות | 678 |
| אטרקציות | 161 |
| בילוי ופנאי | 8851 |
| תיירות ונופש | 8852 |
| כושר וספורט | 4627 |
| ביטוח ופיננסים | 8862 |

`הנחות פלוס` ו-`כרטיס אשראי` **אינם** בעץ הזה — הם מפנים החוצה (ראה סעיף 2).

> ⚠️ **הגבלת קצב.** אחרי כ-4,600 קריאות API מהירות השרת הפסיק להגיב לגמרי
> (הבקשות נתקעות בלי שגיאה, לא 429). ברענון הבא חובה לווסת —
> מומלץ ≤5 קריאות במקביל ו-200ms בין מנות. אל תריץ במקביל לקצירה אחרת.

### איך מוציאים נתונים מהדפדפן החוצה

בהצדעה דורש סשן מחובר, ולכן הקצירה חייבת לרוץ בדף עצמו — אבל אז הנתונים
תקועים בדפדפן. שני נתיבים נבדקו:

- ❌ **POST ל-`http://localhost`** — Chrome חוסם (Private Network Access).
  הבקשה נתקעת בלי לזרוק שגיאה. `scripts/recv.ps1` נכתב לשם כך ולא עבד.
- ✅ **הורדת Blob** — עובד מצוין ובלי הגבלת גודל:
  ```js
  const a=document.createElement('a');
  a.href=URL.createObjectURL(new Blob([JSON.stringify(data)],{type:'application/json'}));
  a.download='behatsdaa-raw.json'; a.click();
  ```
  ואז להעביר מ-`~\Downloads` לתיקיית `data\`.

### היקף הקצירה (2026-08-15)

1,034 צמתי קטגוריה · **14,087 פריטים** · עומק 6 רמות · 0 שגיאות.
מתוכם **3,573 עם פרטים מלאים** (מנגנון מימוש, מחיר, הגבלות) — השאר נעצר
בגלל הגבלת הקצב ומחכה לרענון הבא.

| שורש | פריטים |
|---|---|
| צרכנות | 9,312 |
| תיירות ונופש | 2,569 |
| כושר וספורט | 623 |
| מופעים והצגות | 596 |
| בילוי ופנאי | 427 |
| אטרקציות | 391 |
| מבצעי רכב | 125 |
| ביטוח ופיננסים | 44 |

> **חשוב להבנה:** בהצדעה הוא בעיקרו **חנות** — רוב 14,087 הפריטים הם מוצרים
> בודדים (רהיטים, מסכי טלוויזיה, חבילות מלון), לא "הטבות שצריך להפעיל".
> הערך החוזר נמצא בעיקר ב"בילוי ופנאי > ארוחות", ספא, ואטרקציות.
> **אין בקטלוג הזה סופרמרקטים, דלק או אליאקספרס** (חיפוש "שופרסל" ⇒ אין תוצאות).

---

## 2. Be-Plus — הנחות במעמד החיוב ⭐

**כתובת:** `https://be-plus.co.il`
מגיעים לשם דרך "הנחות פלוס" בתפריט בהצדעה (נפתחת התראת מעבר לאתר צד-שלישי).

זהו **המקור בעל הערך היומיומי הגבוה ביותר**: הנחה אוטומטית במעמד החיוב,
ללא הפעלה וללא קופון — פשוט משלמים בכרטיס. `mechanic: auto`.

> **האתר ציבורי לחלוטין — לא נדרשת התחברות.**
> ולכן הקצירה שלו סקריפטית ולא צריכה דפדפן בכלל:
> `powershell -ExecutionPolicy Bypass -File scripts\harvest-beplus.ps1`

### מבנה הנתונים

```
/component/crm/products?cat_id=<id>&limit=<n>
```

הדף אמנם מרונדר בצד לקוח, **אבל הנתונים המלאים יושבים ב-HTML הגולמי**
כ-JSON בתוך אטריביוט `data` של אלמנט `<products>` (עם escaping של `\u05xx`).
אין צורך לגרד DOM — פשוט regex + `ConvertFrom-Json`:

```
<products ref="products" main="true" data='[{...}]'>
```

שדות לכל בית עסק: `model` (שם), `price` (אחוז ההנחה), `city`, `address`,
`free_text` (תיאור), `id`, `cat_id` (רשימת קטגוריות), `display_cat_id`.

הטקסונומיה המלאה יושבת באלמנט `<filterprods data="...">` באותו דף:
**142 קטגוריות** (22 מהן ראשיות) ו-**565 אזורים**.

> שים לב: דף הבית מציג רק 17 קטגוריות ראשיות. חמש נוספות מתגלות רק דרך
> ה-taxonomy — מכבסות (98), בתי דפוס (215), יודאיקה (219), מסיבות ואירועים (224),
> מוסכים ושרותי רכב (326).

### תוצאות הקצירה (2026-08-15) — **10,512 בתי עסק ייחודיים**

| cat_id | קטגוריה | בתי עסק |
|---|---|---|
| 45 | מסעדות, בתי קפה, בתי מאפה ומזון מהיר | 2,932 |
| 36 | אופנה | 1,401 |
| 81 | צרכנות | 1,353 |
| 48 | טיפוח, קוסמטיקה ועיצוב שיער | 1,081 |
| 41 | לבית ולגן | 998 |
| 61 | מוסכים ושירותי רכב | 574 |
| **74** | **מזון, מינימרקטים וסופרמרקטים** | **455** |
| 53 | ספורט | 356 |
| 89 | מחשבים, סלולר וצילום | 313 |
| 39 | רפואה | 202 |
| 66 | אטרקציות ופנאי | 185 |
| 70 | לימודים, חוגים וייעוץ | 144 |
| 56 | תיירות ונופש | 126 |
| 215 | בתי דפוס והדפסות | 94 |
| 88 | אופטיקה ומכשירי שמיעה | 85 |
| 43 | לילד ולתינוק | 83 |
| 76 | אלקטרוניקה, תאורה ומוצרי חשמל | 73 |
| 219 | יודאיקה ותשמישי קדושה | 31 |
| 224 | מסיבות ואירועים | 26 |
| 98 | מכבסות, שירותי גיהוץ וניקיון | 24 |
| 78 | פארם | 20 |
| 326 | מוסכים ושרותי רכב | 1 |

### התפלגות ההנחות — חשוב להבנה

| טווח | בתי עסק |
|---|---|
| 2–2.9% | 471 |
| **3–3.9%** | **5,809** ← 57% מהמאגר |
| 4–4.9% | 485 |
| 5–5.9% | 2,074 |
| 6–9.9% | 932 |
| 10%+ | 349 |

הגבוהה ביותר: 28.5%. **הזנב הארוך ב-3% הוא רעש** — לכן הדשבורד מסנן
כברירת מחדל ל-5% ומעלה.

### מה *אין* ב-Be-Plus (נבדק מפורשות)

- **אין רשתות סופר גדולות** — לא שופרסל, לא רמי לוי, לא ויקטורי, לא יוחננוף.
  455 ה"סופרמרקטים" הם מכולות שכונתיות, אטליזים, מעדניות ופיצוחים.
- **דלק — 0.5% בלבד** (סונול, seven). זניח.
- **אין אליאקספרס.**
- רשתות מוכרות שכן קיימות: H&M 15%, קפה קפה 8.5%, פיצה דומינו 6%,
  פיצה האט 3.5%, TEN 4%.
- 60 בתי עסק מסומנים `city = "online"`.

---

## 3. max — TAU ובהצדעה (הכרטיס) ⭐

**כתובת:** `https://www.max.co.il/benefits` — **ציבורי לחלוטין**, כולל ה-API.
`powershell -ExecutionPolicy Bypass -File scripts\harvest-max.ps1`

שני הכרטיסים מונפקים ע"י max וחולקים את אותו קטלוג ואת אותה בריכת **פינוקים**
(‎~1 לכל ₪2,000 חיוב, עד 4 בחודש, לפי ת.ז. על פני כל כרטיסי max). פינוק שלא נוצל נמחק.
המספר 4 מאומת מהקונפיג של האתר עצמו (`MAX_FUTURE_BENEFITS_AMOUNT`).

### שני endpoints

```
GET /api/benefits/getLobby
    -> result.navBarCategories[].urlName  = categoryEngName (24 קטגוריות)

GET /api/benefits/getCategoriesLobby?isMobile=false&page=N&loadLobby=true
        &category=<engName>&club=undefined&region=undefined&v=<ver>
    -> result.benefits[] , result.isLast   (פגינציה)
```

> **מלכודת:** התשובה מכילה שני מפתחות שנבדלים רק ברישיות —
> `customerNotEligbleUrl` ו-`customerNotEligbleURL`. `ConvertFrom-Json` של PowerShell
> אינו case-sensitive וזורק שגיאה. הקוצר משנה שם לאחד מהם לפני הפרסור.

### מזהי הקטגוריות

`amfimax` · `mizrahicard` · `happyholiday` · `MAXmoments` · `abroadbenefits` ·
`musicshows` · `attractions` · `movies` · `skymaxpremium` · `standupshows` ·
`BacktoSchool` · `skymax` · `maxBituah` · `MyMAX` · `online` · `giftcard` ·
`vacation` · **`paybacksites`** · `clubbenefits` · `plays` · `fashion` ·
`Mitham` · **`bizplus`** · `tastytreat`

### תוצאות (2026-08-15) — 685 הטבות ייחודיות

| מנגנון | כמות | מה צריך לעשות |
|---|---|---|
| רכישת שובר | 295 | לקנות שובר מוזל באתר ואז לממש |
| תמורת פינוק | 192 | עולה פינוק — והם פגים |
| החזר כספי | 22 | **הרשמה חד-פעמית ל-max PayBack** |
| במעמד החיוב | 12 | כלום |

### ⭐ max PayBack — התשובה לאליאקספרס

`/benefits/paybacksites` — 23 אתרים עם החזר כספי לכרטיס:

**AliExpress עד 9%** · Banggood 10% · strawberrynet 7% · Amazon 6% ·
iHerb 5% · eBay 5% · LastPrice 5% · Agoda 4% · Hoodies 4% · PatPat 5% ·
סופר-פארם אונליין 3% · Nike 3% · Terminal X 2% · Fox Home 2%

**חובה להירשם לשירות פעם אחת.** בלי זה לא נצבר כלום.

### bizplus — "הטבות פלוס"

`/benefits/bizplus` היא רשת הנחות במעמד החיוב באלפי בתי עסק — **אותה רשת של
Be-Plus מסעיף 2**, אבל בכניסה נפרדת. ה-API הרגיל מחזיר 0 עבורה (טעינה אחרת),
ולכן היא לא נקצרת אוטומטית.

**האחוזים לא תמיד זהים בין שתי הכניסות.** דוגמה מאומתת: "לצלם עם יאיר פוטולסקי"
— 28.5% דרך max מול 25% דרך Be-Plus; "אחלה דייב" קיים ב-max ולא ב-Be-Plus.
לפני קנייה גדולה שווה לבדוק את שתיהן. בנוסף, גרסת max מותנית ב**תשלום בנייד**.

---

## 4. ישראכרט — וואן זירו

**כתובת:** `https://benefits.isracard.co.il`

> ⚠️ **חוסם לקוחות שאינם דפדפן.** `Invoke-WebRequest` מקבל 403 גם עם User-Agent
> וערכת headers מלאה (Sec-Fetch, sec-ch-ua וכו') — הגנת CDN. חייבים דפדפן אמיתי.

הדף מוגש מהשרת, וכל הנתונים יושבים ב-**`window.epi.CurrentPage.Benefits`**.
זה מערך גלובלי — **כל דף באתר מחזיר את אותן 319 הטבות**, אז אין צורך לזחול קטגוריות.

```js
window.epi.CurrentPage.Benefits   // 319 רשומות
```

שדות שימושיים: `MobileBenefitName`, `RegularBenefitDesc`, `PremiumBenefitDesc`,
`IsPremium`, `Category`, `LinkUrl`, `BenefitAcquireDescText`, `BenefitConditionText`.

**115 מ-319 מתויגות `IsPremium`** — תלויות במסלול. יש לאמת מול המסלול בפועל.

פילוח: בילוי ופנאי 112 · הורים וילדים 76 · הטבות אונליין 71 · נוסעים לחו"ל 40 ·
כרטיס TOP 14 · אטרקציות 5 · אוכל 1.

### ⭐ הממצאים הכספיים

- **AliExpress — עד 9% קאשבק דרך TOPCASH.** גם כאן, לא רק ב-max.
  מחייב הרשמה ל-TOPCASH ולהתחיל כל קנייה דרך הקישור שלהם.
- **שובר קרפור — 250 ₪ ב-235 ₪.** הסופרמרקט האמיתי היחיד שנמצא בכל המאגר.
- **שובר מסעדות — 500 ₪ ב-425 ₪** (15%).
- נעמן וורדינון — 200 ₪ ב-90 ₪ (55% הנחה).

> **חילוץ נתונים חסום כאן.** הורדת Blob, הורדת data-URL, והעתקה ללוח — כולן
> נחסמות בדומיין הזה. הקטגוריה הכספית חולצה ידנית ל-`data/isracard-online.json`;
> שאר 319 הרשומות ממתינות לשיטת חילוץ טובה יותר.

הטבות **חשבון** וואן זירו (להבדיל מהטבות הכרטיס) זמינות **רק באפליקציה** — אין web.

---

## רענון

### אוטומטי — כל יום ראשון ב-9:00

משימה מתוזמנת ב-Windows בשם `CreditCardBenefits-WeeklyRefresh` מריצה את
`scripts\refresh-all.ps1`, שמרענן את **Be-Plus** ואת **max** ובונה מחדש את
`dashboard.html`. הלוג ב-`data\refresh.log`.

```powershell
Get-ScheduledTask -TaskName 'CreditCardBenefits-WeeklyRefresh' | Get-ScheduledTaskInfo
```

לפרסום מחדש של הגרסה לנייד צריך סשן — הכלי לפרסום Artifact אינו זמין למשימה מתוזמנת.

### ידני — דורש דפדפן מחובר

| מקור | למה ידני |
|---|---|
| בהצדעה (קטלוג המועדון) | התחברות OTP + הגבלת קצב אגרסיבית |
| ישראכרט | חסימת CDN ללקוחות שאינם דפדפן |
| אפליקציית וואן זירו | אין web בכלל — צילומי מסך |

### תזכורת חודשית

אירוע חוזר ביומן Google ב-2 לכל חודש, 10:00 — בדיקת יתרת פינוקים לפני שהם נמחקים,
עם הקישורים להרשמות החד-פעמיות בגוף האירוע.
